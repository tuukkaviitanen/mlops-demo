# Crypto Predict API

## Setup

- Install Python 3.x
- Install following dependencies
```
pip install alpaca-py
pip install statsmodels
pip install prophet
pip install plotly
pip install Flask
pip install waitress
pip install pandas
```
